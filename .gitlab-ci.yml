pages:
  image: node:latest
  script:
    - rm package-lock.json
    - rm yarn.lock
    - npm install --progress=false
    - npm run app:build
    - npm run docs:build
    - rm -rf public
    - mv dist public
  artifacts:
    expire_in: 1 day
    paths:
      - public
  only:
    - main
