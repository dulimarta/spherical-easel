{
  "references": [
    // This is the TS config for running our app
    {"path": "./tsconfig.app.json"},

    // This is the TS config used by vite itself inside a NodeJS environment
    {"path": "./tsconfig.node.json"}
  ]
}